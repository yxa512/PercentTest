<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置点击时字体颜色 -->
    <item android:state_pressed="true" android:drawable="@color/pay_pressed"/>
    <item android:state_selected="true" android:drawable="@color/pay_pressed"/>
    <item android:state_focused="true" android:drawable="@color/pay_pressed"/>
    <!-- 默认颜色 -->
    <item android:drawable="@color/pay_normal"/>
</selector>